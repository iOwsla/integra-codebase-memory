# Contributing

Read docs/architecture.md and docs/project-isolation.md first. Keep core free of adapter dependencies. Every parser fix needs a minimal permanent fixture, an exact failing regression, the fix and a passing full suite. Run lint, typecheck, tests and build before submitting. Never include private repositories or secrets in fixtures. See docs/regression-policy.md.
